# Senha do painel CMS em /admin
CMS_ADMIN_PASSWORD=admin123

# Segredo da sessão (troque em produção)
CMS_SESSION_SECRET=troque-este-segredo

# No Render isso vem do Postgres automaticamente via render.yaml
# DATABASE_URL=postgresql://user:pass@host:5432/salgados_borges
